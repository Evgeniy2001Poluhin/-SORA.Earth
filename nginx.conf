upstream erver {
    listen 80;
    server_name _;
    # Uncomment when TLS certs are mounted:
    # return 301 https://$host$request_uri;

    client_max_body_size 10m;

    # API + SPA proxied to FastAPI (StaticFiles serves SPA inside backend)
    location / {
        proxy_pass         http://sora_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        "";
        proxy_read_timeout 60s;
    }

    # WebSocket / SSE upgrade if used
    location /ws {
        proxy_pass         http://sora_backend;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";
        proxy_read_timeout 3600s;
    }

    gzip on;
    gzip_types text/plain application/json application/javascript text/css image/svg+xml;
    gzip_min_length 1024;
}

# HTTPS server — enable after dropping certs to ./certs/
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     include /etc/nginx/conf.d/default.conf;
# }
